import React from 'react';
import { linkTo } from '@storybook/addon-links';
import Button from './Button';

export default { title: 'Components/IconButton', component: Button };

export const normal = () => <Button>Dit is een button</Button>;
